summary: MCP private research briefs temporarily unavailable
description: >
  Returns MCP_IDENTITY_SCOPE_UNAVAILABLE without database access until the
  Gateway can verify the end-user actor and project membership.
lock: '!inline f/content_research/research_tools/get_research_briefs.script.lock'
concurrency_time_window_s: 0
concurrent_limit: 4
schema:
  type: object
  additionalProperties: false
  properties:
    limit:
      type: integer
      default: 20
      minimum: 1
      maximum: 50
  required: []
timeout: 10
