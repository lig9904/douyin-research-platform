summary: MCP read - research task scopes and runs
description: >
  Read-only bounded list of research-task scope, cadence, next run and latest
  safe run status. It never dispatches, pauses, edits or archives a task.
lock: '!inline f/content_research/research_tools/get_research_briefs.script.lock'
concurrency_time_window_s: 0
concurrent_limit: 4
schema:
  type: object
  additionalProperties: false
  properties:
    limit:
      type: integer
      default: 20
      minimum: 1
      maximum: 50
  required: []
timeout: 10
