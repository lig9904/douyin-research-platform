summary: Dispatch human-reviewed project ASR
description: Fixed schedule-only batch; starts at most 12 previously approved
  project media reviews per run. Never creates or modifies a review and never
  retries a persisted submission.
lock: '!inline
  f/content_research/project_asr/dispatch_pending_reviewed_asr.script.lock'
concurrent_limit: 1
schema:
  type: object
  properties: {}
timeout: 1200
