summary: Recover pending reviewed project ASR tasks
description: Fixed schedule-only batch. Selects at most 12 already-submitted tasks under a 12-poll and 24-hour recovery policy; reports limited or failed tasks as operator attention and never submits new media.
timeout: 1200
concurrent_limit: 1
