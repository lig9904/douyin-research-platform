summary: Dispatch approved ASR first submissions
description: Fixed operator configuration. Skips existing content across model versions. Requires two normal workers; deploy and verify before enabling schedule.
timeout: 1800
lock: '!inline f/content_research/analysis/dispatch_reviewed_asr.script.lock'
concurrent_limit: 1
schema:
  type: object
  properties: {}
