# 037 项目案例核看：测试服发布记录

状态：2026-09-26 已完成测试服研究库加法迁移和单个 Raw App 发布，并从真实研究台回读功能。尚未完成本次隔离恢复演练，也未把任何候选视频标为完整案例；本记录不等于九九业务指导意见验收。

## 发布范围

- 代码源：PR [#155](https://github.com/lig9904/douyin-research-platform/pull/155)，测试服检出固定提交 `30f23b9`，工作树发布前无改动。
- 仅迁移研究库 `037_project_case_review.sql`，仅发布 `f/content_research/research_dashboard` 单个 Raw App；未执行全工作区同步、未更新调度脚本、未创建或激活付费采集任务。
- 新能力是项目私有、追加式的完整视频案例核看，并把后续行动卡绑定到已审案例版本。候选收录、ASR/L3 文本或播放/互动数字均不能单独满足完整案例门槛。

## 数据库与备份

- 迁移前双库备份：`/srv/douyin-research-test/backups/20260926T074952Z`，独立校验返回 `BACKUP_VALID`。
- `test-server-release.sh migrate` 成功，自动备份 `20260926T075044Z`；随后 `verify` 返回 `VERIFIED ... case review contracts and ledger`，覆盖新增表、触发器、函数及迁移账本。
- 迁移后双库备份：`/srv/douyin-research-test/backups/20260926T075141Z`；`test-server-backup-verify.py` 独立校验返回 `BACKUP_VALID`，校验时间为 UTC `2026-09-26T07:54:18Z`。
- 隔离恢复演练尚未执行；不可把备份校验或数据库契约验证称作恢复成功。该演练将只使用两个固定临时库，待单独确认后执行。

## 单应用发布与现场回读

- 测试 Windmill 为 CE v1.815.0；服务器临时目录内从 Node 官方地址获取 v22.16.0 包，经官方 SHA-256 清单校验为 `OK`，安装匹配版本的 Windmill CLI v1.815.0。`wmill app lint` 核验 35 个 backend runnable、Raw App 配置及前端 bundle，返回 `All checks passed`。
- 经用户确认创建一小时令牌，界面复核精确权限为 `apps:write`、`raw_apps:write`、`users:read`。令牌经终端无回显变量交给官方 CLI，仅执行一次目标路径的 `wmill app push`，返回 `Raw app pushed`；终端变量随后清空。令牌值未写入仓库或本文，按用户此前选择等待自动过期。
- Windmill 库只读回查：目标应用从 **48** 个版本、最高 ID **56** 增至 **49** 个版本、最高 ID **57**。刷新真实 Chrome 公开研究台并选择“渔岛 / 九九 IP 号”后，视频库仍加载 **42** 条项目关联记录；已接受视频详情出现“本项目案例核看”面板和核看结论输入。编辑器同时载入 `ProjectCaseReviewPanel.tsx` 与 `manage_project_case_review` runnable。
- 本轮未对任何真实视频提交核看结论；页面出现和应用版本增长证明功能已部署，不证明业务案例已有足够依据。没有触发新的付费接口调用。

## 后续验收边界

1. 完成已备份数据的隔离恢复演练，并回查临时库清理；不覆盖运行库、不删除 MinIO 对象。
2. 从拟做九九 IP 号真正可比的原帖中逐条核对完整画面、声音、互动机制、权利与反证，形成至少不同案例支撑的制作假设。两个既有官方职人号内容形态不适合作为未来 IP 号的直接样板。
3. 再核对页面候选、版本绑定、撤回/更新后的停用行为，并用正式项目行动卡和后续自有发布结果评价，而不是从测试通过推断业务有效。
