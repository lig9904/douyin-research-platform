# 032 项目私有 ASR/L3 业务验收清单

状态（2026-09-24）：032 已发布测试服，项目 A 的真实 ASR→L3→页面链路已跑通；项目 B 同视频公开依据可见、A 私有结果不可见。**业务价值尚未验收**：这一个案例尚不能证明对“九九 IP”或其它账号类型可复用。下述每项区分测试服事实、估算账本和待验收事项。

## 要解决的业务问题

同一公开视频可能被不同项目研究。项目 A 的音频审核、转写、L3 正文审核、分析结论和费用属于 A；项目 B 不应仅因拥有同一视频就看到或复用这些内容。负责人需要能从视频原始依据看到研究结论，判断能否借鉴，并知道当天项目任务的已知费用和待对账项。

| 模块 | 输入 | 输出与边界 | 协作/验收 |
| --- | --- | --- | --- |
| 采集和媒体 | 已纳入并 accepted 的公开视频、真实规范化 WAV | 媒体存在私有对象存储；项目页仅显示有权限资产清单及短期试听 URL，不返回对象 Key | 负责人试听完整音频；确认有充分人声及具体价值，拒绝空泛样本 |
| 项目 ASR 审核 | 指定资产的完整 manifest、服务域名、固定同意语 | 只写该项目人工审核；撤销后不得新提交或继续传播 | 登录用户亲自确认，不能由定时 Worker 自批 |
| 项目 ASR 执行 | 当前审核 ID、受信任存储与火山配置 | 同一任务 Key 最多提交一次；后续只轮询原任务；转写、状态和费用仅写项目表 | 真正调用火山，核对请求标识、音频可取、时长、文本、分段；中断后恢复轮询不重付费 |
| 项目 L3 审核与分析 | 完整转写、公开视频元数据、评论数字统计 | 人工确认固定指纹后调用方舟；不发送评论原文；结果只写该项目 | 人工检查钩子、叙事结构、可借鉴机制、局限，不把模型推断当事实 |
| 页面与决策 | A/B 同视频，项目成员角色 | A 看 A 的内容和费用；B 不见 A 的私有证据；负责人可追原始视频、指标、转写和输出 | 以 A/B/C 登录实际操作，不只读 SQL；撤销后结果不再可见 |
| 费用 | 项目任务账本、执行任务与供应商日账 | 按上海自然日、项目、币种拆实扣/估算/混合；失败、提交后未知和 Worker 中断而未落账的任务均计入“待对账”，不计零、不等于供应商总账 | 核对当日调用次数和本地账本；火山实际账单未接通时明确标注未对账 |

## 真实案例判定

先使用非“九九”试点项目的已知充分人声样本验证完整链路，不创建“九九”业务项目。已有全局《那一刻，我也被净化了》可作为候选，但其历史审核不能自动继承为项目 A 审核。至少保留一个低人声或转写不足的反例，证明系统能拒绝不具研究价值的内容；再选择不同表达类型的第二条真实口播/对话样本，避免从一个成功视频推出普遍有效。

每个正例不仅需要 API 成功，还要由人判断：转写是否抓到核心信息、L3 是否指出具体可复用的开头/叙事/互动机制、哪些结论只是猜测、项目下一步行动是否能落为决策卡并在后续发布数据中复盘。未产生可执行判断时，记录为“技术链路通过、业务价值未验收”。

## 发布与故障门槛

1. 032 源码先进入可复现 Git SHA，Windmill 新脚本依赖固定到该 SHA 并生成锁；先备份、再迁移、执行 `verify`，然后隔离恢复对比 7 张项目私有表的存量。不能把数据库成功等同于 Raw App 发布。
2. 以最小 App Reader 权限实测 A/B/C；后台脚本只接受持久化 ID，配置/Key/媒体 URL 不作为调用参数。
3. 外呼前再次锁定审核；模拟撤销与提交/轮询并发、HTTP 后未知状态、对象丢失、Worker 中断；不能因恢复而重新 submit。同 Key 重放必须零新增外呼。
4. 记录测试日期、视频/项目 ID、审核指纹、Provider 任务号、提交与轮询次数、转写质量、L3 版本、页面截图/读取结果、项目账本和供应商日账状态。证据中不包含 API Key、私有 URL 或完整敏感正文。

测试不设人为金额上限，但继续保留防重复提交、有限轮询、逐次人工审核和费用如实记录这些正确性约束。

## 2026-09-24 测试服实证与剩余门槛

- Draft PR #155 已推送，仍未合并。测试服 Git HEAD 为 `400f266`；032 迁移、9 条独立脚本和 34 个 Raw App 后端已发布。Windmill Python 依赖固定到 `34ce0706b2b937c04bac732413d5809ac271ed9c`，官方锁已生成。本地 Python 3.14 PostgreSQL 针对性测试与 Raw App 打包通过；PR 的 `build`、`test`、`validate` 应以最新提交的检查状态为准。
- 发布前备份为 `/srv/douyin-research-test/backups/20260924T085022Z`，本次修复前再备份 `/srv/douyin-research-test/backups/20260924T093817Z`。032 迁移后的 `/srv/douyin-research-test/backups/20260924T085140Z` 已通过隔离恢复演练，恢复库已清理；无 MinIO 对象删除。
- 项目 A `22021995-a2a3-412d-ba80-e8ba5233889e` 纳入视频 `ed72580d-6ec3-41db-985f-0750e3b62dc7`《那一刻，我也被净化了》。73.1 秒 WAV 的项目专属审核由管理员在试听、用户同意后保存；审核 ID `f69a7336-429b-4716-b995-f22e31d6ff89`、媒体指纹 `9a7e50e4…606b86de`。ASR 只 submit 一次、poll 一次，`project_transcript` 留下 143 字转写，指纹 `1c6ac9f5…e62984`；页面可读。ASR 项目费用为 `unknown`，不能记为零。
- 第一份 L3 审核 `privacy-v1` 指纹 `5534c4ad…6790fda` 对应的任务失败。根因是项目证据中的 PostgreSQL 时间/Decimal 值未转成标准 JSON，方舟请求构造会在 HTTP 前失败；旧任务保持失败，不重放其 Key，费用保留未知待对账。修复和 PostgreSQL 回归测试后，用户重新确认同样正文的新版本 `privacy-v2`，指纹 `45262cd3…5362523`。新任务 `project-l3-v1:fb7ea83c…3cef6b29` 一次模型调用完成，结果 ID `2cdbb663-eec2-4a7e-b93c-123e267e9fa9`，项目账本 LLM 估算 `0.004887 CNY`；页面显示结果和成本口径。方舟供应商实际日账尚未接入，不能以这笔估算充当实扣。
- 结论具有初步内容价值：识别了“困难/挫折→方向/希望→双手合力”的叙事递进和情感钩子；模型也明确承认无评论原文不能判断评论语义、无对照案例不能比较、无视觉证据不能分析镜头、无目标 IP 约束不能判断适配。因此这条是技术链路正例，**尚不是九九 IP 的可执行选题建议**。
- 同一公开视频已另行纳入项目 B `5bb74436-9ad7-4f5c-a134-eab61c3b9b95`。在测试研究台切换项目后，B 仅见公开资料，未见 A 的逐字稿与 L3 结果；测试前后项目账本只有 A 的 ASR/L3 行，B 无对应私有行。当前页面核验使用管理员身份切换项目，不能替代 A/B/C 三个独立登录身份的最终 ACL 验收。
- 测试工作区已创建并启用项目 ASR 提交、ASR 轮询、L3 执行三条计划任务；在启用前手动运行均返回 `selected=0`、零额外外呼。三条任务首次按计划触发分别于 UTC 09:53、09:52、09:54 成功完成，均 `selected=0`；随后 Windmill 队列的下一次时间分别为 UTC 10:03、09:57、10:04。仓库 `.schedule.yaml` 仍默认 `enabled: false`，防止其它环境误启用。

剩余业务验收：以 A/B/C 真实账号完成读写矩阵；给一条不同表达类型的充分口播/对话做第二个真实样本，并保留低人声反例；引入明确的项目/账号目标与对照案例，让“可借鉴机制→选题/行动→发布后指标复盘”成闭环；对账火山实际日消费（或明确供应商无法提供的口径）。上述未完成前，勿把 032 当正式 V1 业务验收。
