# 033 事前实验契约：测试服阶段记录

状态（2026-09-25）：研究库 033 迁移、独立校验和双库隔离恢复已通过；**Raw App 尚未发布**，不能将数据库迁移等同于用户可用。

## 输入与范围

- 来源：Draft PR [#155](https://github.com/lig9904/douyin-research-platform/pull/155) 的固定提交 `b3451261b66a351a56b44d96bb485f54faabc43f`。GitHub 的 build、PostgreSQL test 与 validate 检查通过。
- 目标：测试服务器 `/srv/douyin-research-test/app`、既有 `douyin-research-test` Compose 项目、既有外置反代；不安装本机 Nginx，不变更采集计划、Secret、MinIO 权限或生产环境。
- 测试服 Git 工作树原为干净的 `995c0e6`，已检出固定候选提交；未重建容器。PostgreSQL、Windmill server、两个常规 worker 和 worker_native 均为 running，500G 盘所在 `/srv` 约有 484G 可用。

## 数据库发布与恢复证据

- 发布前双数据库备份：`/srv/douyin-research-test/backups/20260925T035048Z`；独立校验 `BACKUP_VALID`，manifest SHA-256 `d8e614d3c0c84a374304ab22c450cce9eb240228b97e8f3fe922ef7e0d07a527`。
- `scripts/test-server-release.sh migrate` 在执行前自动备份到 `/srv/douyin-research-test/backups/20260925T035129Z`；只应用缺失迁移，输出 `MIGRATED`。随后独立 `verify` 对迁移账本、033 字段、约束、触发器和函数体返回 `VERIFIED`。
- 迁移后备份 `/srv/douyin-research-test/backups/20260925T035209Z` 的 manifest SHA-256 为 `e8c5edca71a1add6246ed352edc2592226f9017e9f239ec796b95692c3fda676`。用它恢复到固定的两座临时库，返回 `RESTORE_DRILL_VALID`、`experiment_contract=present`、`invalid_link_count=0`、`v1_release_accepted=false`，耗时 19 秒。之后查询临时恢复库数量为 0；运行库未被覆盖，MinIO 对象未删除。

## 尚待完成

- Windmill 研究台 `f/content_research/research_dashboard` 仍为迁移前 App：发布前记录到版本数 41、最高版本 ID 49。需要使用受控、短期授权只发布这一 Raw App，再回读版本、权限策略和前后端文件，完成真实页面验收；不执行全工作区同步。
- 当前只到数据库阶段：不能把页面源码已在 GitHub、研究库已迁移或编辑器预览可打开，表述为研究台 033 已生效。发布令牌尚未创建，现有 MCP reader 和旧 CLI token 均未触碰。
- 以 A/B/C 三个独立真实身份验证项目 ACL、事前规则、观察窗、发布凭据和复盘读取。这里不能用管理员切换项目替代。
- 未创建“九九 IP 号”业务项目，也未触发付费采集或生成九九经营结论。权威设定、素材权利、官方账号和真实自有发布仍需后续业务输入。
