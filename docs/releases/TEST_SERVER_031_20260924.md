# 031 行动卡档案绑定：测试服阶段记录

状态：2026-09-24 研究库已迁移并完成隔离恢复；Raw App 尚未发布。因此当前页面仍是 030，不把本记录视为完整 031 上线或 V1 业务验收。

## 输入与变更

- 候选提交：`8ea1b0a47a6414ee56b56b89d1e3c275036dcc10`（Draft PR #154）。测试服务器 `/srv/douyin-research-test/app` 的干净工作树已检出该提交；未重建容器，旧 Windmill App 仍在运行。
- 发布前从 030 状态创建双数据库备份 `/srv/douyin-research-test/backups/20260924T070417Z`。
- 使用 `scripts/test-server-release.sh migrate` 在现有 `douyin-research-test` Compose 项目运行加法迁移 `031_decision_card_profile_binding.sql`；脚本自动生成迁移前备份 `/srv/douyin-research-test/backups/20260924T070448Z`。随后单独运行 `verify`，迁移账本、031 表、外键、触发器和函数体契约全部通过。
- 迁移后备份 `/srv/douyin-research-test/backups/20260924T070616Z` 已在固定的两座临时库中完成隔离恢复，输出 `RESTORE_DRILL_VALID`，`decision_binding_contract=present`，业务关联审计 `full_chain_videos=2`、`invalid_link_count=0`、`v1_release_accepted=false`；脚本确认临时库已清理。没有覆盖运行库或删除 MinIO 对象。

## 尚待完成

- Windmill Raw App 的 `DecisionLoop.tsx`、`decision-loop.css`、`get_project_decision_loop.py`、`mutate_project_decision_loop.py` 仍未发布/回读。页面继续显示旧 030 行为；不能按数据库迁移宣称用户可用。
- 发布后还要用真实 A/B/C 身份验证项目隔离，并用测试主体的“权利未核验”档案验证 `adopt` 被拒；只有得到真实、已核验授权的业务档案才能验收采用路径。当前合成主体不可作为九九经营证据。
- 项目级 ASR/L3、项目成本归属、真实业务素材和自有账号效果回流仍未闭环；本次没有触发任何付费 Provider 调用，也未建立“九九”项目。
