# 030 主体档案版本：测试服发布记录

状态：2026-09-24 已部署至 `test-research`，仅为测试服候选；不代表“九九”业务项目已建立、V1 业务验收或生产发布。

## 输入与范围

- 来源：Draft PR [#153](https://github.com/lig9904/douyin-research-platform/pull/153) 的固定提交 `04fcd9b80b5185f0ffc03c73f435404aad353e78`；目标服务器 `/srv/douyin-research-test/app` 已检出该提交。
- 仅执行研究库加法迁移 `030_subject_profile_version.sql` 并发布 `f/content_research/research_dashboard` Raw App。未同步整个 Windmill 工作区、未更改采集脚本或全局定时任务。
- 未创建“九九”项目或主体，未执行 TikHub、ASR、L3 或其他付费调用。

## 发布与恢复

- 发布前备份：`/srv/douyin-research-test/backups/20260924T051734Z`，独立校验的 manifest SHA-256 为 `cf6478caa9f717bd8194ae64b193fa68ac8d8d5ac44809fccd994b9629fe4a76`；迁移前自动备份：`/srv/douyin-research-test/backups/20260924T052953Z`。
- `test-server-release.sh migrate` 和随后独立运行的 `verify` 均通过；校验包含迁移账本、档案表与索引、所有者及授权、两个触发器绑定/事件类型/函数体 SHA-256。
- 发布前后 Windmill API 原始对象快照保存在本机目录 `/Users/lg-mac/Documents/Codex/2026-09-20/douyin-research-v1/test-server-evidence/` 下的 `research-dashboard-pre-030.json` 和 `research-dashboard-post-030.json`（`0600`，不入 Git）。Raw App 从版本 36 增至 37、保持非草稿；新增 `get_subject_profiles`、`mutate_subject_profile` 两个后端入口，原 28 个入口的策略未变，`extra_perms` 未变，仍以原发布者身份运行。
- 发布后无档案数据的备份 `/srv/douyin-research-test/backups/20260924T053342Z` 已完成隔离恢复。含合成档案的备份 `/srv/douyin-research-test/backups/20260924T054208Z` 再次完成隔离恢复：`RESTORE_DRILL_VALID`、`subject_profile_contract=present`，manifest SHA-256 `119a842bb3cd680d47d20cc5572a285d1b68dbf0d878fbf062ba8b27ebe75052`。两次演练后的固定临时库数量均为 0；未覆盖运行库、未删除 MinIO 对象。
- 若 Raw App 需回退，先恢复 Windmill 应用前一版本并回读非草稿对象；030 数据库迁移是加法变更，常规应用回退不覆盖或降级运行库。前述备份与对象快照用于事故恢复和复核。

## 真实身份验收

- 项目 A 创建合成主体“030 档案验收主体（测试数据）”及一条已核准的 `v1` 档案；权利状态为“尚未核验”，内容明确标注仅供权限验收，不允许被解读为真实业务素材授权。来源为上述本机目录中的 `030-profile-source.md`，文件 SHA-256 为 `0c85e702a32ee4efbc2c80d5bc98b7281fe8caeac5164da81d6ac8d68c88b1f3`。
- Chrome 真实登录：A 仅可选项目 A，页面回读核准档案和管理按钮；B 仅可选项目 B，看不到 A 主体；C 可打开其 App Reader 授权的 Raw App，但显示“没有可访问项目”。验收后 Chrome 已恢复管理员登录，停在项目 A 的研究任务/核准档案页。
- 运行库核对：2 个验收项目、1 个合成主体、1 条已核准档案；名称含“九九”的项目和主体均为 0。未创建研究任务、未触发付费采集。

## 尚未声称完成

- 此次 B/C 验收是实际登录后的页面与项目列表隔离；未把 B 临时加入 A 作为只读成员，也未做恶意篡改 `project_id` 的线上请求。数据库 ACL、读者最小摘要与状态机由本地/CI 测试覆盖，但不替代更完整的现场安全评测。
- 合成档案不能证明“九九”业务内容的价值、来源权利或运营决策闭环。业务项目仍待用户确定资料与授权后另行建立。
- PR #153 与其底座 PR #152 均仍是 Draft；此次仅为测试服部署，不合并或打正式 V1 tag。
