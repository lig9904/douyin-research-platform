# 036 精确视频 ID 入口：测试服发布记录

状态：2026-09-26 已在测试服完成数据库迁移、隔离恢复、单脚本及单应用发布，并从真实研究台回读精确视频 ID 入口。本次发布与其后的独立付费业务试跑分开记账；均不等同于九九业务验收。

## 范围与输入

- 代码源：PR [#155](https://github.com/lig9904/douyin-research-platform/pull/155)，测试服检出固定提交 `43913263aef14add2dcf7a2cecb8407548bf84b3`。
- 本组发布仅含研究库加法迁移 `036`、`f/content_research/collectors/run_research_brief` 调度脚本，以及 `f/content_research/research_dashboard` 单个 Raw App。不执行全工作区同步。
- 精确视频 ID 入口只允许项目内一次性任务，输入 1–20 个不重复数字 ID；初始阶段只采公开详情元数据，不自动采评论、媒体、ASR 或 L3。
- 不创建或激活付费任务，不修改生产环境。

## 数据库发布与恢复依据

- 发布前双库备份：`/srv/douyin-research-test/backups/20260926T041851Z`；独立校验返回 `BACKUP_VALID`。
- `test-server-release.sh migrate` 执行成功，自动备份：`/srv/douyin-research-test/backups/20260926T042000Z`。随后 `verify` 确认研究库迁移账本、项目相关性、决策、私有分析及精确视频任务契约均有效。
- 迁移后双库备份：`/srv/douyin-research-test/backups/20260926T042113Z`。以该备份恢复到固定隔离临时库，研究库与 Windmill 库的关键表计数、迁移与业务关系检查通过；返回 `RESTORE_DRILL_VALID`、`EXACT_VIDEO_RESTORE_CONTRACT=present`，耗时 23 秒。随后独立只读查询确认两个固定恢复库的剩余数量为 **0**；没有覆盖运行库或删除 MinIO 对象。

## Windmill 发布与现场回读

- 发布前研究台 Raw App 历史最高版本 **54**；目标调度脚本 **9** 条版本记录，最新创建时间 `2026-09-24 04:16:09.270783+00`。测试服仓库检出 `43913263`，工作树无改动；脚本文件及 Raw App 目录均存在。临时目录内安装经 SHA-256 校验的 Node v22.16.0 与版本匹配的 Windmill CLI v1.815.0，不更改系统 Node 或服务器 Nginx。
- 经本次用户确认，在测试 Windmill 新建 1 小时 CLI 令牌，权限仅 `scripts:write`、`apps:write`、`raw_apps:write`、`users:read`。令牌仅经无回显测试服终端会话传入 CLI，未写入仓库或本文。`wmill script push` 只更新 `f/content_research/collectors/run_research_brief` 并返回 `Script ... pushed`；Windmill 库回读目标脚本版本数 **10**，最新创建时间 `2026-09-26 04:42:45.351983+00`。最新 `script.content` 包含 `video_ids` 分支，`script.lock` 包含固定依赖提交 `e5291b4f55d3a2470d0dba7d4cf28ea2875cdd69`，数据库布尔查询均为真。
- `wmill app push` 只更新 `f/content_research/research_dashboard` 并返回 `Raw app pushed`；Windmill 库回读目标应用 **47** 条版本记录，最高版本 ID **55**（此前最高 ID 54）。未执行全工作区同步。
- 真实 Chrome 登录会话打开测试研究台、选择“渔岛 / 九九 IP 号”后，“研究任务 → 从哪里找”出现“指定视频 ID”；选择后显示必填的“抖音视频 ID”多行输入与实际请求路径报价提示。当前九九项目原有 5 个任务均不是 `video_ids`，本次未保存草稿、未激活任务或调用付费接口。UI 入口及加载已验证；输入边界的自动化测试已覆盖 1–20 个去重数字 ID，但尚未在现场提交真实 ID 任务。
- 研究库发布前只读统计：`account` 暂停 2 条、`keyword` 暂停 5 条、`low_fan` 活跃 1 条/暂停 1 条；`video_ids` 为 **0**。既有活跃低粉任务不属于本次新建对象，发布脚本时保留其调度语义；本次没有声称该调度周期已再次运行。

## 下一步业务验收

- 先核对精确候选 ID、实际视频内容与 TikHub 本日价格，再在单独的真实业务试跑中创建、激活一次性精确 ID 任务；验证运行记录、项目候选、原始详情绑定、无重复付费及供应商日账。`7521318904971578681` 是已完整审片的机械神兽设施/票价对照，形态接近但叙事目标不同，只能作为有限反例；不要因形态接近自动认定为九九制作方向。
- 页面目前按“主体名称/别名/地域”把部分官方职人号作品判为规则相关；用户已明确这些作品不适合作为未来 IP 号制作参考。规则相关仅是候选路由，不能进入正式制作结论；应补充内容形式/受众可比性人工复核与排除理由，保留原始候选。
- PR 仍为 Draft；本记录不授权合并或正式 V1 发布。

## 后续独立业务试跑（同日，非上述发布步骤）

- 在“渔岛 / 九九 IP 号”创建一次性“对照｜机械神兽展示与景区角色日常”任务，只给两个已人工核看内容的公开视频 ID：`7521318904971578681`（夔牛天街机械兽设施展示）和 `7653684558261710777`（宋城花盆人卡点上班）。任务先保存、后激活；人工只针对该任务运行一次。首次手工传错任务 UUID，流程返回 `deferred`、外部调用 **0**，并未产生视频观察；核对数据库正确 UUID 后重跑，任务于北京时间 **13:02:33** 成功，自动暂停，没有留下重复调度。
- 真正成功的 Windmill 运行 ID `01a0dc17-f820-9e86-f8d2-081c4cbaa9b1`，研究流水 ID `b474eff4-c343-4222-bff6-120962c181b6`。结果：**2** 条观察、**2** 个去重视频、**2** 次未缓存 Provider 调用、**0** 次评论/媒体/ASR/L3、**0** 条自动评分、**0** 条自动相关。两条项目相关性均为 `pending`，规则没有把外形或标题相似直接当成九九适配；项目视频库新增两条候选。研究流水存储 `estimated_api_cost_usd=0.002`、`api_cost_basis=estimated`、`reconciled_api_cost_usd=null`，因此绝不将 USD 0.002 写为实扣。
- 北京时间 **13:13** 只读回看 TikHub 仪表盘“今天”：账户合计 **35 次 API 调用、余额消费 USD 0.1860**，这包含其他任务及账单查询，不能归因于本次两条视频。价格页当时列出的 `fetch_one_video` 单价为 USD 0.0010；本次两次调用的真实分笔扣费仍待供应商使用日志/日账对齐，不用账户总额冒充项目费用。
- 页面和规范化指标快照均显示夔牛视频 149 赞、31 评论、234 分享；花盆人视频 3,994,601 赞、52,851 评论、2,280,649 分享。两条的接口播放数均为 0，页面已标“0 · 待核”；不能计算互动/播放率。花盆人抖音公开页当场显示约 399.5 万赞、5.3 万评论、228.1 万分享，和采集量级一致，但公开显示不能代替供应商账单或受众年龄与回访证据。
- 完整审片后，两条均经研究台“核对实际内容 → 确认纳入”接受为本项目**有限对照证据**：夔牛只回答“机械形态展示不等于角色叙事”；花盆人只回答“清晰角色动作嵌入上班日常可引发观众拟人化解读”。接受公开依据不表示内容可照搬，也不批准云端音频转写或 L3；这些仍需项目逐条审核。
- 真实操作发现视频库按精确视频 ID 搜索返回 0，虽然标题搜索和“纳入已有视频”能找到记录；“运行与成本”页面也尚未列出项目发现费用。这些是本轮试跑实际发现的缺口，后续修复与回读如下。

## 后续单应用修正发布与现场回读（同日）

- 独立复审指出三处费用/检索边界：项目内隐藏的 30 天筛选会挡住历史证据；未知计价口径但非空的数值可能误入“已知金额”；失败发现任务沿用数据库默认 CNY 会被误认作人民币费用。提交 `250d17a` 已处理：项目视频库返回本项目所有未归档记录，跨项目 ACL 不变；全局精确 ID 检索可越过默认时间窗；未知口径金额不参与已知合计，失败发现任务的币种单列 `UNKNOWN`。发现费用按任务启动日归属，页面提醒跨午夜时不等于供应商实扣日。
- PR #155 的最新 `build`、两条 `test`、`validate` 均通过；测试服从 GitHub 取得并检出 `250d17a`，工作树此前无改动。使用官方 Node v22.16.0（下载包与官方 SHA-256 清单匹配）和匹配服务端的 Windmill CLI v1.815.0，在测试服 `wmill app lint` 验证 34 个 backend runnable 与前端构建均通过。
- 经用户单独确认，新建有效期 1 小时、仅含 `apps:write`、`raw_apps:write`、`users:read` 的测试工作区令牌；只对 `f/content_research/research_dashboard` 执行 `wmill app push`，CLI 返回 `Raw app pushed`。令牌仅经无回显终端变量使用，发布后变量已清空；未写入仓库或文档，按本次授权等待自动到期。专用临时 Node/CLI 目录 `/tmp/wmill-app-antPyy` 已清理。未更新数据库、脚本、全工作区或触发新的付费采集。
- Windmill 数据库只读回查：目标 Raw App 版本由 47 条、最高 ID 55 增为 **48 条、最高 ID 56**。真实 Chrome 研究台刷新后，九九项目视频库显示 33 条项目关联记录；按历史视频 ID `7521318904971578681` 检索返回 1 条、状态“已接受”，而非此前的 0。该视频发布于 2025-06-29，证明项目内检索没有被隐藏的近 30 天窗口挡住。
- 同一页面“运行与成本”显示 2026-09-26 的 USD 项目发现任务 **5 条、估算 USD 0.183、实扣空值**；另有币种 `UNKNOWN` 的失败/未落账发现任务 1 条，金额空值。页面明确区分项目任务估算、未知项与供应商日实扣；这不表示 TikHub 已分笔对账，也不得用全账户消费去填项目实扣。
