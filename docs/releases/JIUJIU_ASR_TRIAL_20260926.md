# 九九项目真实 ASR 试跑：短片无人声终态

日期：2026-09-26。范围仅为测试工作区 `test-research` 中的“九九 IP 号”项目和公开视频《师兄去哪了》（抖音 ID `7684244242625316517`）；不代表其他视频或整个项目已完成转写、L3 或制作指导意见。

## 输入与执行

- 视频在本项目为 `accepted`；媒体 Worker 已将其公开来源视频转为私有 MP4 与 WAV。WAV 资产为 `8e52cd4c-b382-4be5-9906-01eed0e90390`，大小 641,366 字节，SHA-256 前缀 `6ca0b1ee8fd9`。研究台可加载“待审核 WAV · 626.3 KB”和试听入口。
- 经项目负责人同意，研究台保存了绑定该资产、项目与 manifest 指纹的音频审核。页面显示“已审核”；此状态只授权把该音频交云端，并非案例核看的“完整核听”证据。案例核看仍记录“声音未核听”。
- 单条受控派发作业 [`01a0dda0-f6c7-a6e3-507c-c8ecbfc7707c`](https://dy.yudao.cc:6443/run/01a0dda0-f6c7-a6e3-507c-c8ecbfc7707c?workspace=test-research) 返回 `submitted`、`created=true`、`external_calls=1`，服务端固定使用火山 ASR；没有记录短时签名媒体 URL。任务键绑定项目、审核、资产及模型版本；本次没有重复提交。

## 可核验结果与费用

- 随后的已提交任务轮询 1 次，数据库 `project_asr_execution_job` 终态为 `failed`，错误码 `project_asr_no_speech`，`submission_count=1`、`poll_count=1`。这表示供应商没有返回可用人声转写，**不等于人工已确认整段绝对无声**；本项目没有该视频的已完成 `project_transcript`，不应继续 L3。
- 费用账本 `project_research_task_cost` 记录 `failed`、`asr_cost=NULL`、`cost_basis=unknown`、币种 CNY。它既不是零实扣，也不是已知报价；最终费用待火山日账核对。不得因无人声终态改任务键重提同一音频来制造虚假成功。
- 此片仍可用于画面与评论机制的有限研究，但缺少完整逐镜/音轨核看、可信播放量及目标客群数据，不能将其点赞和评论归因于台词或人物关系，也不能升级为九九的已验证创作规则。

## 持续授权与下一步

业务需求是对同类已纳入的公开视频音频提供项目级持续云端转写授权，不重复逐条征询。迁移 038 与对应代码已增加独立、可撤销的项目授权事实，将它和“我已逐条核听”明确分离；调用前重查项目、来源、资产指纹与指定火山 Provider，页面将结果标为“未经人工核听/转写未校正”。038 数据库、三个 ASR 脚本和研究台页面已在测试服发布；九九 Owner 已保存持续授权，首条《奥术之贼｜01》火山任务已完成并在项目页面显示转写，详见[现场记录](TEST_SERVER_038_20260926.md)。项目级 ASR 授权不自动满足 L3 正文审核或 037 案例完整核看。

《奥术之贼｜01》已完成媒体入库与真实云端转写，机器稿有实质对白，但语言标为 `zh-CN` 而正文主要为英文，且有明显疑似误识别；不能把未经人工核对的台词当成剧情事实。业务方向还需要完整画面、声音、反证、可比性和至少一个不同形式的对照案例。
