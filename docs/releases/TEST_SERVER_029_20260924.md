# 029 项目级主体评分：测试服发布记录

状态：2026-09-24 已部署至 `test-research`，仅为测试服候选；不代表 V1 业务验收或生产发布。

## 输入与范围

- 来源：PR [#152](https://github.com/lig9904/douyin-research-platform/pull/152) 的固定提交 `dd3e4480afd1cfc1592e5eb1df1f4057188a739d`，不是浮动分支。
- 目标：研究数据库迁移 029、Windmill `f/content_research/collectors/run_research_brief`、`f/content_research/research_dashboard` Raw App。
- 排除：不创建“九九”业务项目、主体或任务；不手动执行 TikHub、ASR 或 L3；不改生产环境。030 主体档案迁移仍只在本地开发中。

## 发布与回滚依据

- 发布前备份：`/srv/douyin-research-test/backups/20260924T040818Z`；迁移前自动备份：`/srv/douyin-research-test/backups/20260924T040920Z`。首份备份通过独立清单与 SHA-256 校验。
- 发布前 Windmill 对象快照：`/srv/douyin-research-test/evidence/research-dashboard-pre-029.json` 与 `run-research-brief-pre-029.json`，均为仅管理员可读；不得放入 Git。
- 发布后对象快照：同目录 `research-dashboard-post-029.json` 与 `run-research-brief-post-029.json`。应用页面可回退到前版对象；迁移 029 为加法变更，不以覆盖运行库的数据库降级作为常规回滚。
- Windmill 锁哈希已在 `dd3e448` 校正，重新发布脚本时不再出现元数据过期提示。

## 接口与协作

- 项目任务需选择已有研究主体；主体相关性先于项目候选的 L1 排序。项目级评分写入 `project_video_subject_score`，不改全局 `video_score`。
- 研究台仍由 Windmill 项目成员身份与数据库项目 ACL 双层判权；管理员在现有测试项目 A 页面可看到主体规则、主体选择和相关候选优先级入口。
- 现有全局定时任务不属于本次变更；发布时未新建项目任务，也未手动触发付费接口。

## 已验收与未验收

- `test-server-release.sh migrate` 成功；发布后 `verify` 返回 029 及其前序迁移账本和约束通过。
- Raw App lint/bundle 通过，页面已在 Chrome 登录态刷新并显示新入口；Windmill API 回读显示应用为非草稿，脚本包含固定代码版本及主体字段。
- 发布时研究库仍为 2 个验收项目、0 个主体、0 条项目主体评分、0 条名含“九九”的主体；因此尚无真实项目评分数据或“九九”业务成果可验收。
- 发布后的备份 `/srv/douyin-research-test/backups/20260924T042111Z` 已恢复到两个固定隔离临时库并通过数据、迁移与业务链校验；`RESTORE_DRILL_VALID`，约 16 秒。临时库清理后再次查询数量为 0，未覆盖运行库或删除 MinIO 对象。
- PR #152 在提交 `dd3e448` 的 GitHub 检查通过，仍是 Draft；未合并或发布正式版本。
