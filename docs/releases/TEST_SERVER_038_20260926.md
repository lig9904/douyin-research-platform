# 038 项目级 ASR 持续授权：测试服阶段记录

日期：2026-09-26。状态：研究库 038 已部署、双库隔离恢复通过；Windmill 脚本与研究台 Raw App 尚未发布，故**不能**宣称持续授权已在页面生效或已触发新的火山转写。

## 范围与目的

- 输入：测试工作区 `test-research` 的已纳入且仍可用的抖音公开视频、可信媒体入库 WAV；项目 Owner 可以一次授予本项目火山 ASR 持续授权，并可撤销。
- 输出：每条待送音频保存与项目、授权版本、资产摘要、来源及 Provider 绑定的独立记录，明确 `not_listened`；不得把授权称作人工逐条核听，不代替 L3 正文审核。
- 发布源：Draft PR [#155](https://github.com/lig9904/douyin-research-platform/pull/155) 固定提交 `ce039259984be9e0a3dd26bf52f22983dbe103a5`。该提交的 GitHub build、test、validate 检查均已通过；测试服检出此提交时工作树干净。

## 数据库现场证据

- 发布前研究库最新迁移为 `037_project_case_review.sql`，测试服旧代码提交 `ca1f4314c63f03b58b4ffdc173e82f8a5a7fce59`。
- 先备份 `/srv/douyin-research-test/backups/20260926T130642Z`，独立校验 `BACKUP_VALID`；`migrate` 又先生成 `/srv/douyin-research-test/backups/20260926T130758Z`，随后成功应用 038。
- `test-server-release.sh verify` 返回 `VERIFIED ... ASR standing grant contracts and ledger`，核验迁移账本、授权表、约束、索引、触发器与函数指纹。
- 迁移后备份 `/srv/douyin-research-test/backups/20260926T130846Z` 独立校验 `BACKUP_VALID`。以此备份执行双库隔离恢复，脚本返回 `RESTORE_DRILL_VALID` 且 `asr_standing_contract=present`；只读检查 `test_server_research_restore` 与 `test_server_windmill_restore` 均已清理，运行库未被覆盖，MinIO 对象未删除。
- 九九项目 `92402a0d-c66b-4c08-b0bc-b1a23c67cd49` 的 standing grant 目前为 0 条，不会因迁移自行产生外呼。下一条候选 WAV `581252f8-a7c5-453a-881c-40f29395733d` 有同视频 MP4 父资产、相同可信存储位置及成功媒体入库记录，仍需应用发布与实际请求结果核验。

## 仍待现场验收

1. 在测试 Windmill 只发布 `dispatch_pending_reviewed_asr`、`dispatch_reviewed_asr`、`poll_reviewed_asr` 三个脚本和 `f/content_research/research_dashboard` 单个 Raw App；回查脚本依赖版本与页面实际版本。
2. 由九九项目 Owner 在页面创建一次持续授权，回读 `authorization_kind=standing_grant`、`listening_status=not_listened`，验证撤销后不再新增提交。
3. 运行一次代表性公开音频 ASR，核对供应商返回、项目私有转写、费用未知/实扣状态、页面展示及 L3 单独审核门槛。不能以此前《师兄去哪了》的 `no_speech` 结果替代这一步。
