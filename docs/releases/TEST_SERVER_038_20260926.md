# 038 项目级 ASR 持续授权：测试服阶段记录

日期：2026-09-26。状态：研究库 038 已部署、双库隔离恢复通过；三个 ASR 脚本与单个研究台 Raw App 已定向发布，九九项目 Owner 已保存持续授权。首条火山 ASR 已真实提交，自动轮询到 `completed`，研究库和页面均有转写；尚无 L3 结论或完整业务案例。

## 范围与目的

- 输入：测试工作区 `test-research` 的已纳入且仍可用的抖音公开视频、可信媒体入库 WAV；项目 Owner 可以一次授予本项目火山 ASR 持续授权，并可撤销。
- 输出：每条待送音频保存与项目、授权版本、资产摘要、来源及 Provider 绑定的独立记录，明确 `not_listened`；不得把授权称作人工逐条核听，不代替 L3 正文审核。
- 发布源：Draft PR [#155](https://github.com/lig9904/douyin-research-platform/pull/155) 固定提交 `ce039259984be9e0a3dd26bf52f22983dbe103a5`。该提交的 GitHub build、test、validate 检查均已通过；测试服检出此提交时工作树干净。

## 数据库现场证据

- 发布前研究库最新迁移为 `037_project_case_review.sql`，测试服旧代码提交 `ca1f4314c63f03b58b4ffdc173e82f8a5a7fce59`。
- 先备份 `/srv/douyin-research-test/backups/20260926T130642Z`，独立校验 `BACKUP_VALID`；`migrate` 又先生成 `/srv/douyin-research-test/backups/20260926T130758Z`，随后成功应用 038。
- `test-server-release.sh verify` 返回 `VERIFIED ... ASR standing grant contracts and ledger`，核验迁移账本、授权表、约束、索引、触发器与函数指纹。
- 迁移后备份 `/srv/douyin-research-test/backups/20260926T130846Z` 独立校验 `BACKUP_VALID`。以此备份执行双库隔离恢复，脚本返回 `RESTORE_DRILL_VALID` 且 `asr_standing_contract=present`；只读检查 `test_server_research_restore` 与 `test_server_windmill_restore` 均已清理，运行库未被覆盖，MinIO 对象未删除。
- 迁移时九九项目 `92402a0d-c66b-4c08-b0bc-b1a23c67cd49` 的 standing grant 为 0 条，未因迁移自行产生外呼。随后 Owner 在新版研究台明确授权，新增一条 `active`、Provider 为 `volcengine-doubao-asr` 的项目记录；不把它写成“已逐条核听”。候选 WAV `581252f8-a7c5-453a-881c-40f29395733d` 有同视频 MP4 父资产、相同可信存储位置及成功媒体入库记录。

## Windmill 发布与真实调用回读

- 经用户确认，创建有效期 1 小时的测试工作区令牌，界面实际核对为 `scripts:write`、`apps:write`、`raw_apps:write`、`users:read`，到期时间为北京时间 22:24。令牌未写入仓库或本文；CLI 调用结束后已清空系统剪贴板。仅向 `test-research` 定向推送 `dispatch_pending_reviewed_asr`、`dispatch_reviewed_asr`、`poll_reviewed_asr` 与 `f/content_research/research_dashboard`，CLI 分别返回脚本 pushed 与 `Raw app pushed`，没有同步整个工作区。
- 测试服 Windmill 库只读回查，三个脚本的最新创建时间依次为 `13:25:57`、`13:26:02`、`13:26:03 UTC`，锁文件均含固定依赖提交 `5c72d8cd048fd266e1cc592a07964b5d018925a5`；派发脚本最新版还包含 standing grant 查询。研究台版本由 53 条、最高 ID 61 增至 **54 条、最高 ID 62**。真实 Chrome 页面刷新后，九九项目视频详情显示持续授权操作，保存后显示“项目持续授权生效 · 未逐条核听”；数据库只读回查授权状态为 `active`，该视频审核为 `authorization_kind=standing_grant`、`status=approved`，JSON 声明中的 `listening_status=not_listened`，不冒充人工核听。
- ASR 派发与轮询两个现有计划在 Windmill 库中均为 `enabled=true`，分别为 UTC 每 10 分钟与每 5 分钟。手动触发一次最新派发脚本的 [运行记录](https://dy.yudao.cc:6443/run/01a0dde8-b871-bcd5-efbc-c20271c339af?workspace=test-research) 返回 `selected=1, submitted=1, completed=0, needs_attention=0`。研究库记录该条《奥术之贼｜01》的任务由 `submitted` 经自动轮询 `running`、`poll_count=1`，再到 `completed`、`poll_count=2`；`project_transcript` 为 1 条、正文 1,104 字符、音频 285,071 ms。项目页面显示 `bigmodel`/`2.0`/`volc.seedasr.auc` 及机器稿，标注“原音频未经人工核听”。这证明真实人声转写链路运行，不证明转写准确或案例完整。
- 同一视频的 `project_research_task_cost` 为 `asr_transcription/completed`，`asr_cost`、`api_cost`、`total_cost` 均为 `NULL`，`cost_basis=unknown`；页面明确显示“未知 / 待对账”，不是零费用。仍需与火山实际日账核对。
- 发布后发现项目 Owner 在中心 `l3_privacy_reviewers` 名单中且成员角色正确，但研究台返回的 `can_review_l3=false`；刷新页面后仍复现。`get_my_projects` 的运行依赖锁缺少它用来读取 Windmill 变量的 `wmill` 包，代码异常路径静默拒绝审核。此问题已在本地补依赖并加定向回归测试，须重新发布单个 Raw App 并现场复验；不能以改名单或绕开服务端审核解决。
- 发布时曾从仓库根目录试推送，CLI 返回无效远端路径，未形成该次目标发布；随后从 `windmill/` 目录第一次调用被仓库本地 `wmill.yaml` 解析到 `content-research-local`，使**本机**同名派发脚本多出一个版本。发现目标错误后改为显式 `--workspace test-research --base-url`，上述三脚本和应用才发布到测试服。该本机版本属于同一已测试源码，不是测试服证据；本次未主动触发本机任务或付费调用。

## 仍待现场验收

1. 发布 `get_my_projects` 运行依赖修复并验证 Owner 的 `can_review_l3` 恢复；再对 L3 正文单独生成、核对和批准。该授权不得从 ASR 持续授权推断。
2. 对新资产/撤销边界做隔离验证；不要为验证撤销而打断当前持续授权。首条 standing-grant 媒体审核记录与未核听标记已现场回读。
3. 核对火山实际日账；机器稿尤其英文台词须对照完整原片核听。完整视频案例、可比性和反证不能由 ASR 成功替代。
