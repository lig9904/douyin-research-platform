# L3 中文与证据约束复验

## 现场问题与修改

首次真实分析虽技术完成，但输出英文，并从评论数字统计推断用户意图、虚构对照比较及平台机制。保留原 `l3-prompt-v1` 结果，不覆盖或把它当作业务质量通过。

新 system prompt 版本 `ark-l3-evidence-boundary-zh-hans-v2-2026-09-21` 要求简体中文；没有评论原文、对照、视觉、平台机制和目标 IP 时明确证据不足。JSON 字段、请求指纹、模型响应绑定及隐私审核保持原契约。

这是提示约束，不是保证中文或事实正确的语义验证器。现有响应校验仍仅验证结构与绑定，后续真实输出必须人工逐项检查；不能仅凭 completed 启用无人值守研究结论发布。

## 部署与验收

- 手动 `run_reviewed_l3` 的源码及锁一致固定至 `e33b56c11359e41e1ceee3957391ef3ef59e3478`，只替换项目源码版本，第三方依赖保持原锁；部署后须验证实际运行。
- 执行前将既有 Secret 配置的 `prompt_version` 显式设置为上述版本，其他键、费用策略保持不变；版本参与任务键，保留旧结果。
- 使用同一已批准指纹的正文进行一次真实复验，不重复采集和转写。
- 查看七个分析栏目，检查中文、统计与语义区别、缺失对照/视觉/IP/平台机制，以及费用 estimated 标记。重复入口应零外部调用。
- 自动 L3 分发仍未启用；本次仅更新手动入口，不声称其他 Worker 的固定依赖已升级。新提示仍待真实质量复验。

## 真实复验结果（北京时间 08:45 后）

PR #93、#94 已通过对应 CI、独立审查并合并。测试服务器 HEAD 为 `e1fe1ec43e0a09f9ce0200cb5c83a9536da304a3`；手动 L3 入口与研究台已发布，日志 `/srv/douyin-research-test/evidence/deploy-e1fe1ec-20260921.log` 退出 0。配置只更新 prompt_version，回读核对其他字段不变，Secret 属性保留。

同一已审核指纹完成一次真实复验：分析 ID `d0a074a1-06c3-4117-a5ae-5adc2ec5f5b0`，completed、attempt_count=1、external_calls=1、sdk_retries=0。数据库独立联查确认新旧两个版本分别留存、指纹一致。新版估算费用 0.004996 CNY；两次业务 L3 合计估算 0.011082 CNY，不含合成探测、ASR，也不是供应商实际日账。日志 `/srv/douyin-research-test/evidence/l3-quality-v2-20260921.log`。

在真实页面逐项阅读并截图确认：七个栏目使用中文；没有评论原文就不推断用户语义，没有对照就不比较，没有平台机制证据就不推断推荐权重，缺失视觉和目标 IP 均明确标注。这一条样本的首测越界问题未在复验中重现，但并不保证其他输入，也不是最终用户验收。

信息价值仍不足：此样本只有短点名转写和统计，大多栏目只能合理返回“证据不足”。下一步应改进候选信息充分性判断及受控证据补充，不继续反复调用同一输入，也不能将保守空结论包装为可服务业务的研究报告。

重放日志 `/srv/douyin-research-test/evidence/l3-quality-v2-replay-20260921.log`：created=false、external_calls=0、attempt_count 仍为 1，未重复模型调用。旧首测质量问题继续保留。
