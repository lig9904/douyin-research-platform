# 抖音研究平台 V1 收尾任务书

更新日期：2026-09-21

执行负责人：Codex 主流程；用户负责最终业务验收及必须由本人完成的授权。

目标：让研究台在测试服务器上持续产出可用的真实研究结果，而不是仅有页面、模拟数据或单独接口测试。

## 一、交付定义

必须完成同一批真实业务数据的闭环：

发现视频 → 详情和指标入库 → L1 排序 → 按规则采集评论及生成 L2 特征 → 下载媒体、提取音频、存入私有 MinIO → 云端转写 → 合格候选进入 L3 分析 → 结果入库 → 研究台查看证据、转写、分析及每日费用。

不是所有视频都必须调用模型。每条视频必须有可解释的处理状态、跳过原因或失败原因；不得把真实缺失值伪装为零，也不得伪造转写、审核或分析结果。

V1 完成必须同时满足：
1. 测试服务器真实链路通过，不用本机模拟结果替代。
2. 用户能在研究台看到并使用结果，无需操作脚本。
3. 定时任务不依赖浏览器登录，重复执行不重复付费。
4. 每日费用可见，未接入或未知金额明确标注。
5. 代码、迁移、配置说明和验收记录可追溯到 GitHub 提交及部署版本。
6. 完成备份恢复与回滚验证，再发布正式 V1；测试通过不自动等于用户验收。

## 二、当前基线

最新增量（2026-09-21，PR #100 部署后）：服务器代码 `c53e22a`，迁移 018 已执行，研究台与查询库的合并指标已实际生效；真实样本播放 18,860,155、点赞 1,481,962、粉丝 1,918，来源依据默认折叠。原始快照及 L1 时序评分不变。证据见 [合并指标验收](MERGED_METRICS_V1.md)。

- T5 定时证据：UTC 02:10 和 03:10 两个连续 schedule 周期均成功，发现/评论/媒体批次绑定已核验，媒体均复用已有对象。第二周期发现外部调用 1 次，不代表整轮免费。详见 [定时周期证据](SCHEDULED_RESEARCH_CYCLE_V1.md)。该流程不自动批准音频或正文，不代表 ASR/L3 自动链路通过。
- T1 费用：最近核验 TikHub 快照为 USD 0.159，抓取 UTC 02:00；未包含此后全部调用，不是终账。火山实际日账仍未接入，模型估算与 ASR 未知费用已明确区分。
- T6：经用户确认，服务器以 `e7849ce` 完成备份 `20260921T024519Z` 的隔离数据库恢复，退出 0，耗时 8 秒；业务关联 1 条、异常 0。两个指定临时库已清理，运行库与容器存续。见 [实际恢复证据](SERVER_RESTORE_2026-09-21.md)。MinIO 实物、应用回滚及完整配置/权限恢复仍未验收，不能因此发布正式 V1。
- T6 后续实物核验：6 个 MinIO 对象共 10,790,476 字节，流式 GET 大小及 SHA-256 全部一致；原备份媒体引用七字段与当前库 6 条记录完全一致。日志与边界已补入实际恢复证据。此项不代表对象丢失恢复、应用回滚或完整配置/权限恢复通过。
- 业务价值：已有一条审核后的真实 ASR/L3，但材料不足，不能冒充有价值的完整研究结论。候选内容充分性、完整业务使用验收、恢复/回滚及正式 V1 发布仍未完成。

以下记录为历史检查点；不得将其中较早的部署版本或“尚未”状态当作当前状态。

最新增量（北京时间 08:45 后）：PR #93/#94 合并并部署至 `e1fe1ec`，中文证据约束提示完成一次真实复验，页面逐项检查本样本未再出现首测越界结论；新版估算 0.004996 CNY，重放零调用，旧记录保留。由于样本信息贫乏，大多栏目返回证据不足，仍不能算业务价值验收通过。下一步补候选信息充分性与受控证据输入，避免对贫乏输入重复付费；自动业务连续周期、火山实际日账、恢复/回滚仍待完成。

最新增量（北京时间 2026-09-21 08:17 后）：PR #92 已合并并部署至 `f66ea30`。用户音频审核与精确 L3 正文确认已保存；一条真实 ASR 和一条真实 L3 已完成、落库并在研究台显示，二者重跑均不新增外部调用。ASR 费用未知；L3 估算 0.006086 CNY，不是供应商账单。实际 L3 内容出现英文和超出证据的推断，**质量验收未通过**，正在版本化约束后复验。ASR 轮询两个空队列定时周期成功，不代表业务全链路连续运行。火山实际日账、业务自动调度、恢复/回滚及最终验收仍待完成。以下旧基线保留历史，不覆盖此增量。

最新增量（UTC 2026-09-20 23:45）：PR #90 已合并并部署至 `691acf5`；3 条真实视频的 MP4/WAV 共 6 个对象已入库，首条对象复用、播放器加载及签名链接续签已验证，临时目录已清空。用户反馈首条几乎无人声，故不代表音频内容质量、转写适用性或用户验收通过；三条音频尚未保存人工审核，ASR 未提交，T4/T5 未通过。TikHub 当前账期累计 USD 0.106，供应商同步、数据库及首页一致；新数据库备份完整性校验通过。ASR Secret 已设置，音频人工审核、真实 ASR/L3、业务连续调度、恢复及回滚仍待完成。下表保留早期基线，具体最新证据以 [服务器验收检查点](SERVER_ACCEPTANCE_2026-09-21.md) 首节为准。

| 模块 | 已有证据 | 尚未完成 |
| --- | --- | --- |
| 测试环境 | 独立反代、研究台、数据库、双普通 Worker 已配置；应用与数据位于 /srv 的 500G 盘 | 新增功能部署后仍须现场核验 |
| 基础采集 | 测试服真实采集 3 条视频、快照及 L1 评分，页面可查 | 定时运行与连续周期验证 |
| 私有存储 | HTTPS 上传 1644 字节测试 WAV、签名下载及内容校验通过；匿名和篡改签名返回 403 | 真实视频媒体任务、服务器内网上传、页面播放 |
| 费用页面 | 真实日账同步及页面曾核对；现场确认小时计划连续三个周期成功，最新账单已落库 | 页面最新显示、跨日切换及其他实际使用服务的费用仍待验 |
| S3 与媒体 | 两个普通 Worker 已部署 ffmpeg，500G 盘临时目录可写；媒体脚本完整锁已发布 | 固定存储配置、真实媒体处理、复用、清理及页面播放 |
| ASR / L3 | #83–#86 已合并；五个分析脚本已发布，锁非空且无锁错误；迁移至 017 | 固定配置、审核页面完整发布、真实执行及计划启用，无完整服务器验收 |

早期基线代码为 `9d142d735b6be67b6ea24800770bc148498961c2`，最新现场代码为 `691acf5cbe9e7f9a25331981ee34b88973195604`。代码、运行时、脚本与页面的部署范围分别核验，不以仓库 HEAD 代替全部服务版本。证据及边界见 [服务器验收检查点](SERVER_ACCEPTANCE_2026-09-21.md)。禁止输出 Secret 内容。

## 三、执行工作包

### T0：收敛分支和审查问题（P0）

- 输入：最新主分支、已合并 ASR Worker 与审核页面、独立审查意见。
- 工作：保留既有修改；分离费用、存储、分析接入的提交；修复审核身份不得由请求方自授、媒体审核必须可追溯的问题。不得直接部署当前未审查的服务层。
- 输出：边界清楚的 PR、回归测试、部署顺序和回滚点。
- 接口/协作：主流程决定服务端身份与审核记录契约；子任务只做独立探索、机械验证或只读审查。
- 验收：调用方无法通过自填 actor/allowlist 取得权限；缺失或不匹配媒体审核记录时不触发外部调用；CI 通过且主流程实际复核。用户取消金额上限不等于取消身份验证。

### T1：每日实际费用上线（P0，可独立交付）

- 输入：TikHub Secret、研究数据库、供应商当日用量响应。
- 工作：部署迁移 015、费用同步脚本和页面；检查并复用已有同路径计划，启用每小时同步；手动运行一次。
- 输出：首页当日累计、每日列表、更新时间及失败/过期提示。
- 接口：GET /api/v1/tikhub/user/get_user_daily_usage；supplier_daily_spend；Windmill 同步任务。
- 验收：供应商响应、数据库和页面金额及账期一致；失败不写零；历史快照标明未终账；USD/CNY 分开；刷新费用不触发视频采集。
- 后续：为实际使用的火山服务接入日账来源；阿里仅在启用其业务调用时接入。暂不能取得实际账单时明确未同步，不用估算冒充实扣。
- 边界：用户只需知道每天花多少钱，不新增逐笔对账和按任务分摊工程。

### T2：真实媒体进入 MinIO（P0）

- 输入：真实已入库视频的合法下载信息、现有 S3 凭据、私有桶。
- 工作：实现下载、音频提取、文件指纹、对象入库、失败状态和临时文件清理；凭据保存到受控 Secret；上传走内网，外部读取使用 HTTPS 签名。
- 输出：与视频绑定的媒体元数据，研究台可播放/查看，转写任务可取得短时有效音频链接。
- 接口：S3 适配器、媒体记录、Windmill 媒体任务、研究台媒体展示。数据库保存对象 key，不长期保存临时签名 URL。
- 验收：服务器处理真实媒体成功；下载前后摘要一致；重复处理复用文件；失败不标记成功；匿名访问拒绝；签名过期能够重新生成；无密钥或签名写入日志。
- 边界：不将管理控制台对外发布；不设置公开桶；不新增应用服务器 Nginx；不删除已有用户媒体。

### T3：评论、L2 和候选晋级接通（P0）

- 输入：已采集视频、真实评论、已有特征提取器和候选规则。
- 工作：补齐服务端任务入口和研究台状态；将 L2 特征、候选选择及来源证据持久化。
- 输出：每个视频的分析资格、优先级及原因，不合格项明确跳过。
- 接口：评论采集任务、CommentFeatureExtractor、L3PromotionGate、研究台审核入口。
- 验收：同一批数据可从页面追溯到评论与特征；重复运行不重复创建相同结果；无需浏览器登录即可运行定时任务。
- 协作：数据探索可与 T2 并行，涉及相同数据库迁移和状态机的修改由主流程串行集成。

### T4：ASR → L3 → 页面闭环（P0，依赖 T0/T2/T3）

- 输入：已审核的真实音频、持久化候选及证据、受控 Worker 身份、火山配置。
- 工作：部署真实 ASR 与 L3 任务入口；持久化提交、轮询、转写和分析状态；研究台展示内容及证据，支持安全恢复失败任务。
- 输出：与原视频对应的真实转写、结构化分析、可追溯证据和任务状态。
- 接口：火山 ASR/Ark Provider、执行协调器、研究数据库、研究台详情页。
- 验收：中断后恢复轮询不重复提交；同一输入重复触发不重复付费；审核与输入变更能正确失效；未知费用不写零；成功结果持久化并在页面可见。
- 边界：不测试 qwen-flash；不做无业务价值的大批模型对比；保留证据和身份边界，不重建用户已取消的默认金额上限。

### T5：自动运行和业务验收（P0，依赖 T1–T4）

- 输入：完整已部署链路、业务采集规则、固定的小规模测试批次。
- 工作：先运行至少 3 条真实视频，至少 1 条合格且有语音的视频完整经过媒体、ASR、L3 和页面；其他记录保留真实状态，不强迫无效视频进入模型。
- 输出：可查看的真实研究案例及持续任务运行记录。
- 验收：
  - 同一案例的视频 ID、媒体、评论、转写、分析和页面结果一致。
  - 至少观察两个连续调度周期，并完成一次受控失败恢复验证。
  - 服务端任务不依赖人保持网页登录。
  - 重跑不重复采集同一证据、不重复付费提交。
  - 页面没有把“未采集/未知”当作真实 0，错误有明确提示。
  - 用户可以完成“找视频 → 看依据 → 看分析 → 查看每日费用”的实际使用流程。
- 顺序：真实付费链路串行进行，避免并行测试重复消耗；无需付费的独立验证可并行。

### T6：运维与 V1 发布（P1，闭环通过后）

- 输入：验收通过的提交、迁移、运行配置和数据备份。
- 工作：检查任务失败可见性、存储与临时文件增长、数据库与媒体引用备份；在隔离目标验证恢复；形成可执行回滚记录；更新 GitHub 文档、发布说明与版本。
- 输出：明确部署版本、验收报告、恢复/回滚记录和 V1 发布。
- 验收：备份可实际恢复且媒体引用有效；回滚路径不覆盖未知用户数据；发布说明明确已完成与已延期内容。没有相应证据不得宣称通过。
- 边界：不擅自发送真实通知；需要外部通知渠道时先准备方案，再由用户确定接收方。

## 四、并行与提交规则

1. 主流程负责架构、集成、部署、费用调用和最终验收。
2. 可以并行：只读审查、独立模块测试、互不改同一文件的文档与实现工作。
3. 不并行：同一文件、同一数据库迁移序列、同一业务状态机、同一服务器发布、真实付费链路。
4. 子任务“完成”必须经主流程实际复核，不直接作为验收证据。
5. 每个工作包维护四个独立状态：已实现 / 已测试 / 已部署 / 实际生效。
6. 每次部署记录完整 SHA、迁移、验证结果和回滚点；不把浮动分支名当作版本证据。
7. 不以覆盖其他人的修改、跳过测试、伪造数据或放宽权限来追求进度。

## 五、范围与用户参与

- 现有服务器、反代、域名、500G 盘和 MinIO 已具备，不要求用户重复搭建。
- 优先让抖音真实链路可用，不扩展多平台、控制台重做、无必要的模型评测或复杂费用分摊。
- 已授权的小规模、有效付费测试继续按业务需要执行，清楚记录费用；遇到必须操作时确认的工具规则照常遵守。
- 仅遇到缺失配置、实质扩大权限/数据公开范围、真实通知接收方或影响业务结果的选择时请求用户决策。
- 不承诺没有现场证据支持的完成百分比或截止时间；按上述工作包和验收结果报告进度。

## 六、下一次执行顺序

历史代码交付检查点（以下“尚未”仅表示当时状态，最新部署以第二节和服务器验收记录为准）：

- 已合并 #73 媒体入口、#74 评论批次、#75 ffmpeg Worker 运行时、#76 ASR 审核绑定、#77 L3 自动执行、#78 批次失败收尾、#79 评论调度入口。合并不是服务器部署。
- 已针对 ASR/L3 和评论 Worker 修复跨日记账问题；L3 账本与完成状态之间的失败恢复可重放，不重复调用模型或重复结算。隔离数据库与合成 Provider 验证通过，真实付费运行仍未验收。
- #75 CI 实际构建镜像并验证 Python 3.13、16 kHz 单声道 PCM 转换；服务器 ffmpeg 与 500G 临时目录尚未核验。
- #80 音频预览/试听/审核页面已合并（4ea3a916ea9fe8c5139ba743bbe8f1d67e068c0a）。18 项媒体审核及入口测试、前端构建、Windmill lint、远端 CI 通过，独立审查确认依赖与同步元数据阻断已关闭。浏览器窗口不可用，未实际验收页面或播放。
- #81 ASR 资产输入与固定配置 Worker 已合并（3bb9d854f19b62f7fe08a62d6bf6af25c6e86c56），42 项定向测试及远端 CI 通过；包括原账期恢复、缺失旧账本停止、新任务显式不限额策略。独立审查无 P1 阻断，尚未部署。
- 下一步接 L3 Worker 与轮询调度；服务器可操作后执行媒体 → 真实转写 → 分析 → 页面闭环。T5 连续周期、T6 恢复/回滚与正式发布仍未完成。

当前收尾检查点（2026-09-21，取代尚未完成时的旧执行顺序）：

1. **补齐固定配置**：用户解除浏览器文件上传权限阻点后，导入已有凭据并配置媒体、审核/自动身份、ASR、L3，不输出密钥。
2. **补齐页面并验证单次执行**：入口与调度脚本已发布，保持自动计划未启用；部署审核页面，真实验证媒体、评论、特征与候选。
3. **串行跑真实闭环**：一条合格有语音视频完成媒体、审核、转写、分析及页面，再扩至三条样本；检查费用与重跑复用。
4. **验证无人值守运行**：单次通过后启用相关计划，完成两个业务周期及一次受控失败恢复。TikHub 费用三个周期已通过，不重复替代业务调度验证。
5. **恢复与发布**：隔离恢复、回滚和用户实际验收通过后发布 V1；已有归档完整性校验不代表恢复通过。

每个工作项的完成记录必须包含：输入视频或批次 ID、代码 SHA、测试结果、部署版本、真实输出及剩余缺口。只读审查和独立测试可以并行；同一数据库状态机、服务器部署与付费调用串行。

T0 审查收敛 → T1 费用上线，同时完成 T2/T3 的独立准备 → T2/T3 集成 → T4 真实分析闭环 → T5 连续自动运行验收 → T6 发布。

首个交付点：研究台可查看每日实际费用。

核心交付点：研究台可查看一条真实视频的完整媒体、转写和分析。

最终交付点：无需用户盯着页面，也能持续出结果，并具备可验证的费用与恢复能力。
