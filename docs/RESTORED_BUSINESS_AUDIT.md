# 恢复库业务关联审计

`scripts/test-server-restored-business-audit.py` 只读检查已存在的
`test_server_research_restore`。通过环境变量
`TEST_SERVER_RESTORED_RESEARCH_DSN` 提供连接；不要把连接串写入日志或提交。
工具不创建、不恢复、不删除数据库，也不调用供应商接口。

检查媒体、转写、分析、执行任务和费用记录的关联，并统计同一视频下同时存在
已完成 ASR 与 L3 记录的数量。输出仅含聚合计数，不含正文、签名 URL 或凭据。
退出码 0 表示发现业务链记录且未发现所检查的关联错误；非零表示不满足或检查失败。
若归档包含迁移 020，审计还要求 `research_brief` 与 `research_brief_run` 同时存在并
输出各自行数，`research_brief_contract=present`；迁移前归档输出 `legacy_absent` 且不
声称研究任务行数。两张表的存在状态与迁移账本不一致时检查失败。

## 证据边界

关键表行数应与已校验备份中的 COPY 记录比较，不与持续运行的源库比较。
`test-server-archive-counts.py` 只输出按固定顺序排列的计数，缺少段、重复段或截断时拒绝。
CI 在备份后给运行库增加一条视频，再恢复旧备份，以覆盖这一差异。
这只核对指定关键表行数，并非全库逐行等价或 MinIO 内容校验。

- `business_chain_present` 不是恢复验收通过；`v1_release_accepted` 始终为 false。
- `legacy_absent` 只证明旧版 schema 的回滚备份可恢复，不能作为当前研究任务功能的灾备证据。
- 同视频关联不证明某次 L3 实际使用了某份转写，还需核对输入指纹和审核证据。
- 不验证 MinIO 对象存在、内容校验和、权限、备份前后逐行一致或页面可用性。
- 不与运行中的数据库计数强行比较：备份后采集会使两者合法地不同。
- 恢复演练在清理之前通过 `test-server-restored-business-sql.py` 生成共享审计 SQL，使用现有容器内 psql 执行；不要求服务器额外安装 psycopg。固定恢复库校验与只读事务仍然强制执行，未通过时不得输出恢复成功标记。原有临时库清理边界不变。

## 2026-09-21 本地验证

9 个单元测试通过，包含运行时连接库不符、只读事务未建立、缺表时关闭连接且不查询业务数据。在本地 PostgreSQL 隔离测试库中，以显式只读事务执行全部
17 条聚合/关联 SQL，均成功；数组参数真实适配成功，七张所需表均存在。
该验证仅执行 SQL，不绕过工具固定恢复库限制，不构成服务器实际恢复证据。
服务器恢复、对象核对与回滚验收仍待执行。

后续加固定向测试共 20 项通过（设置本地隔离库 TEST_DATABASE_URL，未跳过）：
真实 PostgreSQL 会话临时表验证正确链路为 1，并逐项验证图片冒充音频、L1 冒充 L3、L3 任务键、L3 输入指纹、ASR 源指纹、ASR 审核指纹错配时为 0；另有真实列约束下任务键错配及生成器拒绝错误目标库测试。所有测试事务回滚，不清空共享表。

审核指纹与 ASR job.media_ref_fingerprint 必须一致。当前检查要求有效审核；如果审核已撤销，历史任务不能作为当前有效链路样本，这不等于历史执行从未合法完成。此工具不重新计算审核指纹，也不验证 MinIO 实物，仍须独立验收。
